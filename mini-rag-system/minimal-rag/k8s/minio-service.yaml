apiVersion: v1
kind: Service
metadata:
  name: minio-headless
  namespace: retrieval-system
  labels:
    app: minio
    component: storage

spec:
  type: ClusterIP
  clusterIP: None

  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: api

  selector:
    app: minio
apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: retrieval-system-yaml
  labels:
    app: postgres
    component: database
spec:
  type: ClusterIP
  clusterIP: None

  ports: 
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres

  selector:
    app: postgres
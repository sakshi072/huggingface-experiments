apiVersion: v1
kind: Service
metadata:
  name: retrieval-api
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "retrieval-system.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "retrieval-system.api.selectorLabels" . | nindent 4 }}
